apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gokube-node-reader
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: gokube-node-reader
subjects:
  - kind: ServiceAccount
    name: gokube-scheduler
    namespace: gokube
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: gokube-node-reader
